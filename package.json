{
  "name": "aigmv2",
  "version": "1.0.0",
  "private": true,
  "description": "Polska gra multiplayer z AI Mistrzem Gry",
  "main": "server.js",
  "scripts": {
    "start": "node --require ./tts-cache-preload.js server.js",
    "check": "node --check server.js && node --check tts-cache-preload.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1"
  }
}
